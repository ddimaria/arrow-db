[workspace]
members = [
    "arrow-db-client",
    "arrow-db-core",
    "arrow-db-server",
    "arrow-db-wasm"
]

resolver = "2"

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"